<div>
  {{#with main}}
  <main>
    <section>
      {{> title big=true data='My educationss'}}
         <ul>
           {{#each education}}
             <li>
              {{name}}

              {{#if isEnd inkludeZero=true}} ({{isEnd}}) {{/if}}
              {{#unless isEnd includeZero=false}} (ще в процесі) {{/unless}}
              </li>
           {{/each}}
         </ul>
       </section>
       <section>
        {{> title big=false data='My sertificates'}}
         <ul>
          {{#each sertificates}}
          <li>
            {{name}}
            {{#if isTop includeZero=true}} ({{id}}) {{/if}}
          </li>
          {{/each}}
         </ul>
       </section>
     </main>
   {{/with}}
  </div> 